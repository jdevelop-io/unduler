[package]
name = "parser-conventional-wasm"
version = "0.1.0"
edition = "2024"
description = "Conventional Commits parser plugin for Unduler (WASM)"

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.41"

[profile.release]
opt-level = "s"
lto = true
